apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alertmanager-webex-teams.fullname" . }}-test-connection"
  labels:
    {{- include "alertmanager-webex-teams.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alertmanager-webex-teams.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
